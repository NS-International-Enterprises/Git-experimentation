name: Enterprise CI Workflow

on:
  pull_request:
    branches:
      - main
permissions:
  contents: read
jobs:
  build:
    name: Build Application
    runs-on: arc-runner-set
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
