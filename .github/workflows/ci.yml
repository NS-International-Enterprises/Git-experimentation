name: CI workflow

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run Hello
        run: echo "Hello, World!"
      - name: Current Date
        run: date
      - name: List all files
        run: ls -la